import { ExpandCollapseDirective } from './expand-collapse.directive';

describe('ExpandCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
